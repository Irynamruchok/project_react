import React from 'react';

const Search = () => {
    return (
        <div>
            <input type={"search"}/>
            <button type={"submit"}> Search</button>
        </div>
    );
};

export default Search;